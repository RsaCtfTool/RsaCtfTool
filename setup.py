# This file has been replaced by pyproject.toml (PEP 517/518).
# It is kept here as a historical reference only and is no longer used.
raise RuntimeError(
    "setup.py is deprecated. Use 'pip install .' which reads pyproject.toml instead."
)
